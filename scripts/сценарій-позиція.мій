; Сценарій-фікстура для CP-PLAYER-WORLD-POSITION
; Після появи opaque `гравець` і (клас ...) читаємо позицію.
; У грі очікується structured value (x y z) або exact numbers.
; Поза грою — oracle-заглушка.

(за-умовою
  ((гравець-присутній?)
   (запиши-лог)
   ; (позиція гравець) або (читати-властивість гравець "WorldPosition")
   ())
  (t ()))
